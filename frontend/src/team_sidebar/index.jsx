import { Box } from "@mui/material";

export default function TeamSidebar$() {
  return (
    <Box
      sx={{
        background: "#242424",
        height: "100%",
        borderRadius: "5px",
        color: "white",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      Team Sidebar
    </Box>
  );
}
